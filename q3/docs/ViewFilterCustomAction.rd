=begin
=ViewFilterCustomアクション

カスタムフィルタを適用します。実行すると、[カスタムフィルタ]ダイアログを開いてフィルタを指定します。フィルタについては、((<フィルタ|URL:Filter.html>))を参照してください。


===[カスタムフィルタ]ダイアログ

カスタムフィルタを指定します。

((<[カスタムフィルタ]ダイアログ|"IMG:images/CustomFilterDialog.png">))


+[条件]
カスタムフィルタの条件をマクロで指定します。


+[編集]
条件を((<条件エディタ|URL:ConditionEditor.html>))で編集します。


==引数
なし


==有効なウィンドウ・ビュー
*メインウィンドウ

=end
