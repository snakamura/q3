=begin
=起動

==メールボックスディレクトリの指定
インストーラでインストールしていない場合には、初回起動時に以下のような((<[メールボックス]ダイアログ|URL:MailFolderDialog.html>))が表示され、メールボックスディレクトリを指定するように求められます。メールのデータを保存したいディレクトリを指定して[OK]を押してください。

((<[メールボックス]ダイアログ|"IMG:images/MailFolderDialog.png">))


==初期画面
起動すると以下のようなウィンドウが表示されます。

((<初期画面|"IMG:images/InitialWindow.png">))

各ウィンドウやビューについては、((<画面構成|URL:WindowsAndViews.html>))を参照してください。以降の説明では、そこで説明されたウィンドウやビューの名前を使用します。

初期状態ではまだ一つもアカウントがないため、まずアカウントを作成する必要があります。

=end
