=begin
=@Identity

 String @Identity()


==説明
コンテキストアカウントの現在のサブアカウントの同一性値を返します。指定されていない場合には空文字列を返します。


==引数
なし


==エラー
*引数の数が合っていない場合
*コンテキストアカウントがない場合


==条件
なし


==例
 # 同一性値を取得
 @Identity()

=end
