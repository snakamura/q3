=begin
=なぜ自分が送信したメッセージを受信するのですか?

デフォルトでは作成したメッセージのBccに自分自身のメールアドレスが含まれるようになっているため、自分が送信したメッセージを受信することになります。これは、POP3を使用して複数のPCでメールを管理するときに、自分の送信したメッセージを管理できるようにするためです。

この機能を無効にするには、((<その他2の設定|URL:OptionMisc2.html>))で[自分をBccに含める]のチェックを外してください。

=end
