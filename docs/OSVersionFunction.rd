=begin
=@OSVersion

 String @OSVersion()


==説明
OSの名前とバージョンを返します。例えば、「Windows XP Service Pack 2」のような文字列が返されます。


==引数
なし


==エラー
*引数の数が合っていない場合


==条件
なし


==例
 # OSの名前とバージョンを取得
 @OSVersion()

=end
