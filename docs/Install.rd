=begin
=インストール

QMAIL3をインストールするには、二通りの方法があります。一つ目はインストーラによるインストールで、Windows版（x64版を除く）で利用することができます。二つ目はZipファイルによるインストーラですべてのプラットフォームで利用することができます。

Windows CE版にはインストーラはありませんので、Zipファイルによるインストールをする必要があります。

*((<インストーラによるインストール|URL:InstallWithInstaller.html>))
*((<Zipファイルによるインストール|URL:InstallWithZip.html>))

=end
