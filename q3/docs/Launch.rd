=begin
=起動

==メールボックスディレクトリの指定
インストーラでインストールしていない場合には、初回起動時に以下のようなダイアログが表示され、メールボックスディレクトリを指定するように求められます。メールのデータを保存したいディレクトリを指定して[OK]を押してください。

((<メールボックス|"IMG:images/MailFolderDialog.png">))


==初期画面
起動すると以下のようなウィンドウが表示されます。

((<初期画面|"IMG:images/InitialWindow.png">))

初期状態ではまだ一つもアカウントがないため、まずアカウントを作成する必要があります。


==ウィンドウ・ビューの名前
アカウントを作成する前に、各ウィンドウ・ビューの名前を説明します。

// TODO

=end
