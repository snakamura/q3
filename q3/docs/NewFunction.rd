=begin
=@New

 Boolean @New()


==説明
自動振り分けで振り分け中でコンテキストメッセージが新着メッセージの場合にTrueを、それ以外の場合にはFalseを返します。自動振り分けで既存のメッセージを振り分ける設定の場合に、この関数を使用することで新着メッセージかどうかを判断することができます。


==引数
なし


==エラー
*引数の数が合っていない場合


==条件
なし


==例
 # 新着メッセージかどうか調べる
 @New()

=end
