=begin
=ViewZoomアクション

HTML表示の文字サイズを指定します。引数には、0から4までの数字、または「+」「-」を指定します。0から4までの数字を指定するとそのサイズにします。0が最も小さく4が一番大きくなります。「+」を指定すると文字サイズをひとつ大きくします。逆に「-」を指定すると一つ小さくします。引数を指定しないとデフォルトのサイズにします。


==引数
:1
  文字サイズ。0から4まで、または+か-


==有効なウィンドウ・ビュー
*プレビュー
*メッセージビュー

=end
