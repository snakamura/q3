=begin
=グループの購読

今のところQMAIL3ではニュースグループの一覧を取得する機能がありませんので、購読するグループの名前は別途用意してください。購読するグループを指定するにはフォルダを作成します。このとき、フォルダ名としてグループ名を指定します。

フォルダを作成するにはメニューから、((<[フォルダ]-[作成]|URL:FolderCreateAction.html>))を選択します。

((<[フォルダの作成]ダイアログ|"IMG:images/TutorialNntpCreateFolderDialog.png">))

[名前]にはグループ名をしていします。[タイプ]は[ローカルフォルダ]を指定し、[同期可能]にチェックが入っていることを確認してください。[OK]をクリックするとフォルダが作成されます。同期すると、指定したグループのメッセージが、作成したフォルダに格納されます。

購読するグループごとに以上の手順を繰り返します。

=end
